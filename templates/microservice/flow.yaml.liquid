apiVersion: deployments.plural.sh/v1alpha1
kind: Flow
metadata:
  name: {{ context.name }}
  namespace: gitops-example
spec:
  name: {{ context.name }}
  description: Basic stateless microservice for {{ context.environment }}.
