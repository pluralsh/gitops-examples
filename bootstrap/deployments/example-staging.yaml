apiVersion: deployments.plural.sh/v1alpha1
kind: ServiceDeployment
metadata:
  name: example-staging
  namespace: gitops-example
spec:
  namespace: example
  name: example-staging
  flowRef:
    name: example
    namespace: gitops-example
  repositoryRef:
    kind: GitRepository
    name: gitops-example
    namespace: infra
  git:
    ref: main
    folder: deployments/helm/example
  helm:
    url: oci://ghcr.io/pluralsh/example-charts
    chart: stateless
    version: 0.1.0
    valuesFiles:
      - base.yaml
      - staging.yaml
  cluster: demo-dev01
